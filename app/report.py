"""
report.py — turns a verdict + evidence into the final report shown
to the user (as JSON now, and later also as Markdown for the CV story).
"""


def build_report(verdict: dict) -> dict:
    """TODO: shape `verdict` into a clean, consistent report dict."""
    raise NotImplementedError
