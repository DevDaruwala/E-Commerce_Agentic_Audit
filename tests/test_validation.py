"""
Starter test file. Run with: pytest

TODO (Phase 1, part of build task 2): write tests using the 3
deliberately broken listings from the fail-forward exercise —
missing field, wrong type, empty string. Assert that validate_listing
catches each one.
"""

from app.validation import validate_listing


def test_placeholder():
    # Replace this once ProductListing and validate_listing are real.
    assert True
